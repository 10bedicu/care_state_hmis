# Encounter Kind Location Assignment Extension Plugin

Adds extra location field for encounter to allow assigning encounter to kind locations.

## What It Does

- Registers a `PlugExtension` for the encounter resource.
- Adds `location` fields to the stored extension payload.
- Defines both write and retrieve schemas for the extension.

## Configuration Notes

This plugin does not define plugin-specific settings in this repository.
