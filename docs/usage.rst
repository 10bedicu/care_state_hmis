=====
Usage
=====

To use Care State HMIS plugins in a CARE deployment, configure the required
plugin entries in your ``plug_config.py`` and install the repository from a
local path or Git URL as described in the readme page.

If you need a Python import in code::

    import care_state_hmis
